dependencies {
    compile project(':core')
}

jar {
    manifest {
        attributes(
                "Main-Class": "edu.technopolis.matrix.cli.MatrixMultiplication"
        )
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}